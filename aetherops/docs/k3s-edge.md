# K3s at the edge

Install [K3s](https://k3s.io/) on edge nodes (factory gateways, city cabinets). Use this repo’s container images and `infra/k8s` manifests. For production, pin versions, enable etcd snapshots or external datastore, and wire NATS/Kafka to your central region.
